# 劳动用工与合同审查 AI 助手设计规格

**日期：** 2026-06-05

**产品定位：** 面向小微企业、HR 与行政人员的劳动用工与合同审查 AI 助手原型。

**目标：** 让非专业法务用户可以完成合同风险审查、版本对比、劳动用工问答、法条依据查询与模板使用，并保留真实的用户数据记录。

---

## 1. 产品目标

本产品原型不再定位为泛法律聊天页面，而是聚焦企业常见的合同与用工合规场景。用户进入系统后，应当能够明确感知到这是一套“企业法务工作台”，核心价值是：

1. 上传合同后得到结构化风险审查结果
2. 对比合同版本后识别关键修改与潜在风险变化
3. 对劳动用工问题获得带法律依据的辅助回答
4. 使用与收藏常见企业文档模板
5. 在个人工作区查看自己的历史审查、问答与收藏记录

成功标准：
- 首页信息架构体现企业法务工作台定位
- 用户数据不再仅依赖 `localStorage`
- 合同审查结果具备固定结构，可供前端渲染、导出与历史回放
- 聊天、收藏、分析、对比至少完成统一用户绑定

---

## 2. 用户画像与核心场景

### 2.1 目标用户
- 小微企业负责人
- HR
- 行政
- 无专职法务的业务负责人

### 2.2 高频场景
1. 上传劳动合同或保密协议，检查是否存在明显风险
2. 对比修改前后的合同版本，确认变更是否引入不利条款
3. 询问劳动法问题，如试用期、竞业限制、加班费、解除劳动合同
4. 查找模板并基于模板继续审查
5. 回看曾经分析过的合同、问答和收藏的法条

---

## 3. 产品结构

### 3.1 首页：企业法务工作台
首页从“聊天首页”调整为“企业法务入口页”。

应包含：
- 核心功能入口：合同审查、文档对比、劳动问答、模板中心
- 最近记录：最近审查、最近对比、最近问答
- 风险提示或使用建议
- 产品免责声明

设计原则：
- 第一屏让用户理解产品用途
- 聊天不是唯一入口，而是工作流中的一个模块
- 首页内容以“任务驱动”而不是“闲聊驱动”组织

### 3.2 合同审查页
保留上传能力，但输出改为更标准的报告视图。

结构包括：
- 文档信息
- 风险总览（高/中/低风险数量）
- 总体风险等级
- 风险列表
- 每项风险的命中位置、法律依据、建议
- 导出报告
- 建议人工复核提示

### 3.3 文档对比页
对比结果不只是文案拼接，而是企业可理解的差异结果。

展示内容：
- 原文 / 修改后片段
- 变更类型（新增、删除、修改）
- 摘要
- 如条件允许，可增加“风险变化提示”字段

### 3.4 劳动用工问答页
问答模块保留，但定位改为专业辅助问答。

输出建议结构：
- 结论
- 适用条件
- 法律依据
- 风险提醒
- 建议下一步

系统不承诺提供法律意见书，而是提供合规辅助建议。

### 3.5 模板中心
模板中心围绕劳动用工场景组织。

优先分类：
- 劳动合同
- 保密协议
- 竞业限制协议
- 离职证明
- 录用通知 / Offer

每个模板至少包含：
- 名称
- 分类
- 说明
- 使用次数
- 使用入口

### 3.6 个人中心
个人中心应从静态菜单变为真实数据看板。

展示内容：
- 用户昵称
- 审查次数
- 问答次数
- 最近使用时间
- 收藏入口
- 历史记录入口

---

## 4. 数据模型与记录策略

### 4.1 用户体系
采用“匿名用户 + 本地持久化 user_id”的轻量方案，不做真正微信登录。

原因：
- 当前原型阶段优先建立真实数据流
- 不引入第三方登录复杂度
- 可在后续替换为微信登录而不破坏主体结构

要求：
- 首次进入时由后端创建匿名用户
- 前端保存 `user_id`
- 后续聊天、审查、对比、收藏全部显式带 `user_id`

### 4.2 合同审查记录
`AnalysisDB` 继续使用，但补齐产品原型所需字段语义。

至少保证返回：
- `id`
- `user_id`
- `document_name`
- `status`
- `risks`
- `summary`
- `created_at`
- `completed_at`

可扩展字段：
- `document_type`
- `overall_risk_level`
- `overall_score`

### 4.3 对比记录
`ComparisonDB` 继续使用，统一绑定用户。

至少保证返回：
- `id`
- `user_id`
- `document_a`
- `document_b`
- `changes`
- `summary`
- `created_at`

### 4.4 聊天记录
`ChatSessionDB` 与 `ChatMessageDB` 已存在，需修正为真实按用户过滤。

要求：
- 创建会话时写入 `user_id`
- 列表查询按 `user_id` 过滤
- 详情查询只能读取当前用户自己的会话
- 发消息时更新用户统计

### 4.5 收藏记录
`FavoriteDB` 继续使用，但前端不再只依赖浏览器缓存。

至少支持：
- 收藏法条
- 收藏问答会话
- 收藏模板

返回列表时应提供足够元信息，避免前端只能拿到 `item_id` 却没有标题。

---

## 5. API 调整设计

### 5.1 用户 API
新增或重构为以下能力：
- `POST /api/user/guest-login`：创建或续用匿名用户
- `GET /api/user/profile/{user_id}`：读取用户概况
- `GET /api/user/dashboard/{user_id}`：读取首页聚合数据
- `GET /api/user/favorites/{user_id}`：返回带标题的收藏列表
- `POST /api/user/favorites`：新增收藏
- `DELETE /api/user/favorites/{favorite_id}`：取消收藏

推荐：保留旧 `login` 接口兼容，但前端迁移到 `guest-login`。

### 5.2 聊天 API
重构请求体，使之显式带用户信息。

- 创建会话：`POST /api/chat/sessions`
  - 请求体至少包含 `user_id`, `title`, `context`
- 会话列表：`GET /api/chat/sessions?user_id=...`
- 会话详情：`GET /api/chat/sessions/{session_id}?user_id=...`
- 发送消息：`POST /api/chat/sessions/{session_id}/messages`
  - 请求体至少包含 `user_id`, `role`, `content`

### 5.3 文档 API
- `POST /api/document/analyze`
  - 表单中增加 `user_id`
- `POST /api/document/compare`
  - 表单中增加 `user_id`
- `GET /api/document/analysis/{analysis_id}`
  - 增加用户归属校验
- 可新增 `GET /api/document/records?user_id=...`
  - 供历史页和首页读取最近审查记录

### 5.4 搜索 API
现阶段保持不大改，但要开始服务劳动用工场景。

改进方向：
- 搜索空关键词时返回热门/常用条款，而不是直接报错
- 搜索结果优先覆盖劳动法、劳动合同法相关内容
- 详情页提供可收藏、可提问、可追踪来源字段

---

## 6. 审查结果结构化规范

合同审查结果必须满足固定结构，以支持前端展示、报告导出和历史记录回放。

推荐结构：

```json
{
  "id": "analysis_xxx",
  "document_name": "劳动合同.docx",
  "status": "completed",
  "overall_risk_level": "medium",
  "overall_score": 72,
  "high_risk_count": 1,
  "medium_risk_count": 2,
  "low_risk_count": 3,
  "risks": [
    {
      "id": "risk_001",
      "level": "high",
      "title": "竞业限制缺少经济补偿",
      "description": "...",
      "location": "第12条",
      "legal_basis": ["劳动合同法第二十三条"],
      "suggestion": "...",
      "suggested_revision": "..."
    }
  ],
  "summary": "..."
}
```

如果 AI 结果缺字段，后端负责做兜底归一化，而不是把脏结构直接交给前端。

---

## 7. 前端状态与交互设计

### 7.1 登录态
前端不再只有 `isLoggedIn` 布尔值，而是至少维护：
- `isLoggedIn`
- `userProfile`
- `userId`

### 7.2 首页数据
首页不再硬编码“最近对话”，改为加载工作台数据。

建议展示：
- 最近审查记录
- 最近问答记录
- 收藏数量
- 累计审查次数
- 快捷功能入口

### 7.3 历史页
历史按类型分组展示：
- 审查记录
- 对比记录
- 问答记录

### 7.4 收藏页
收藏页按类型过滤：
- 法条
- 模板
- 问答

### 7.5 报告页
报告页增强为真实报告视图：
- 风险汇总卡
- 风险分组
- 分析总结
- 导出文本报告
- 免责声明

---

## 8. 错误处理与可信度边界

### 8.1 产品免责声明
在首页、报告页或个人中心至少一处固定展示：
- 本系统结果仅供参考
- 重大决策请咨询专业律师
- AI 分析可能存在遗漏

### 8.2 API 错误处理
统一错误体验：
- 上传失败
- 文档解析失败
- AI 返回失败
- 用户记录加载失败
- 收藏失败

前端不得吞掉关键异常，应给出可理解提示。

### 8.3 AI 边界
AI 回答必须定位为辅助，不得宣称提供正式法律意见。

---

## 9. 分阶段实现建议

### 阶段一：底座产品化
目标：从页面 demo 变成真实可用原型。

范围：
1. 匿名用户体系
2. 用户绑定的聊天/审查/对比/收藏
3. 首页重构为企业法务工作台
4. 历史页与收藏页真实化

### 阶段二：核心能力可信化
目标：让输出更稳定、可解释。

范围：
1. 审查结果 schema 固化
2. AI 输出归一化与兜底
3. 聊天回答结构优化
4. 报告页增强与免责声明

### 阶段三：劳动用工垂直化
目标：让原型更像面向 HR 的产品。

范围：
1. 模板分类聚焦劳动用工
2. 劳动法相关内容优先展示
3. 问答预设问题与提示语聚焦 HR 场景
4. 风险标题和建议用企业可读语言表达

---

## 10. 第一轮实现范围

为控制节奏，第一轮只落这些改造：

1. 新增匿名用户登录接口与前端接入
2. 聊天、审查、对比、收藏改为真实绑定用户
3. 首页替换为企业法务工作台基础版
4. 用户看板、历史与收藏改为后端驱动
5. 审查结果增加基础风险汇总信息

这一轮不做：
- 真微信授权
- 支付与订阅
- 多角色权限
- 复杂向量知识库增强
- 自动修复合同正文

---

## 11. 需要修改的主要文件

### 后端
- `backend/app/api/user.py`
- `backend/app/api/chat.py`
- `backend/app/api/document.py`
- `backend/app/models/database.py`
- `backend/app/models/schemas.py`
- `backend/app/main.py`

### 前端
- `frontend/index.html`
- `frontend/js/app.js`
- `frontend/css/style.css`

---

## 12. 验收标准

完成第一轮后，应满足：

1. 首次进入应用能自动创建匿名用户并保存身份
2. 用户上传合同后，分析记录与该用户绑定
3. 用户创建聊天后，只能看到自己的聊天历史
4. 收藏法条后，刷新页面仍能从后端读回
5. 首页能显示真实的最近记录与统计摘要
6. 报告页能稳定展示结构化分析结果并导出
7. 产品整体观感更像“企业法务助手”而不是“泛聊天 demo”
