# 法律学习 AI 工作台 — 交互原型 v1

> 版本：v1  
> 日期：2026-06-10  
> 作用：在信息架构、低保真结构、组件清单、数据模型、接口草案、前端页面树基础上，定义三大核心页面的关键交互路径、状态切换与动作反馈，供原型设计、前端实现与联调用

---

## 1. 文档目标

本文件用于回答以下问题：

1. 用户进入三个核心页面后，最关键的操作路径是什么？
2. 每个页面的主操作、次操作、系统反馈如何组织？
3. 页面在 loading、empty、error、processing、success 等状态下应如何表现？
4. 前端在实现时需要预留哪些交互容器、抽屉、弹层、确认流和异步反馈？

---

## 2. 原型范围

本版覆盖以下 3 个核心页面：

1. 工作台首页
2. 材料详情 / 阅读页
3. 案件分析工作区

本版不输出高保真视觉稿，而输出：

- 页面目标
- 核心用户任务
- 主路径流程
- 关键交互点
- 状态切换
- 弹层 / 抽屉 / 侧栏行为
- 成功与失败反馈

---

## 3. 全局交互约定

## 3.1 操作优先级

所有页面统一分为三层动作：

1. **主动作**：推动当前任务前进
2. **次动作**：补充、整理、关联、切换视图
3. **风险动作**：删除、归档、覆盖、批量替换

### 交互原则

- 主动作固定在显著位置
- 次动作收纳进工具栏、更多菜单或侧栏
- 风险动作必须二次确认

---

## 3.2 异步反馈约定

所有请求类动作统一经过 4 段反馈：

1. `idle`
2. `submitting`
3. `success`
4. `error`

### 表现建议

- `submitting`：按钮 loading、局部骨架或局部遮罩
- `success`：轻提示 + 局部内容刷新
- `error`：就近报错 + 提供重试

---

## 3.3 页面级状态约定

每个核心页面统一支持：

- `initial-loading`
- `partial-loading`
- `empty`
- `error`
- `ready`

### 原则

- 首屏不可用时用页面级 skeleton / error
- 局部刷新时只刷新局部模块，不整页闪烁
- 页面内多个区块允许分区降级，不要求“一处失败全页失败”

---

## 3.4 右侧栏约定

右侧栏只承担以下 4 类角色之一：

1. AI 协作
2. 关联对象
3. 当前对象补充信息
4. 当前动作面板

### 原则

- 同时只强化一个主上下文
- 不让右侧栏抢中间主内容的注意力
- 可折叠，但折叠后保留再次打开入口

---

## 4. 页面一：工作台首页

## 4.1 页面目标

首页要帮助用户在 10 秒内完成以下之一：

1. 继续已有工作
2. 开始新工作
3. 回看最近沉淀结果

---

## 4.2 核心用户任务

### 任务 A：继续最近案件

用户进入首页后，最快速地回到上次工作现场。

### 任务 B：上传新材料

用户带着新文档进入系统，希望立即开始整理与分析。

### 任务 C：新建案件

用户已有明确分析对象，希望建立新的案件工作区。

### 任务 D：查看近期成果

用户想快速回看最近分析、知识或成果沉淀。

---

## 4.3 页面主路径

## 路径 A：继续最近案件

```text
进入首页
→ 看到“继续工作”区
→ 浏览最近案件卡片
→ 点击某案件卡片
→ 进入案件分析工作区
```

### 关键交互

- 卡片显示案件标题、阶段、最近更新时间、最近动作
- 悬停显示 `继续分析`
- 点击卡片整块可进入工作区
- 若案件存在未完成分析，可显示强调标识

### 成功反馈

- 进入 `CaseWorkspacePage`
- 默认恢复到该案件最近活跃分析或默认分析视图

---

## 路径 B：上传新材料

```text
进入首页
→ 点击“上传材料”
→ 打开上传抽屉 / 弹层
→ 选择文件
→ 填写基础元信息（可选）
→ 提交上传
→ 上传成功后进入材料详情页
```

### 上传弹层字段建议

- 文件选择
- 材料标题（可自动带出，可编辑）
- 材料类型（可选）
- 来源（可选）
- 备注（可选）

### 状态切换

- `idle`：等待选择文件
- `file-selected`：显示文件名与大小
- `uploading`：显示进度与禁用重复提交
- `success`：提示上传完成，自动跳转
- `error`：显示失败原因与重试按钮

### 成功反馈

- Toast：`材料上传成功`
- 跳转到 `/materials/:materialId`

---

## 路径 C：新建案件

```text
进入首页
→ 点击“新建案件”
→ 打开新建案件弹层
→ 输入标题
→ 选择类型 / 阶段（可选）
→ 提交
→ 创建成功后进入案件工作区
```

### 最小字段

- 案件标题

### 建议字段

- 案件类型
- 当前阶段
- 一句话目标

### 状态切换

- `idle`
- `validating`
- `submitting`
- `success`
- `error`

### 表单规则

- 标题必填
- 标题长度超限即时提示
- 成功后关闭弹层并跳转

---

## 路径 D：查看近期对象

```text
进入首页
→ 浏览最近材料 / 最近成果 / 最近知识预览
→ 点击对象卡片
→ 进入对应详情页
```

### 关键交互

- 每个分区只显示少量高价值条目
- 提供 `查看更多`
- 卡片上显示对象类型与最近时间

---

## 4.4 页面状态设计

## 首页 `initial-loading`

- Hero 区骨架
- Continue 区卡片骨架
- Recent 区列表骨架

## 首页 `empty`

适用于新用户或无数据用户。

### 空状态内容

- 欢迎文案
- `上传第一份材料`
- `创建第一个案件`
- 简短说明系统如何工作

## 首页 `partial-empty`

允许局部空状态：

- 最近案件为空
- 最近成果为空
- 最近知识为空

不影响其他区展示。

## 首页 `error`

- 页面顶部错误提示
- `重试加载`
- 若局部模块失败，仅在模块内展示失败态

---

## 4.5 首页组件交互要求

### `DashboardPrimaryActions`

- 两个主按钮必须始终可见
- 在窄屏时允许纵向堆叠

### `DashboardContinueCard`

- 支持整卡点击
- 支持 hover 高亮
- 支持显示最近分析进度摘要

### `DashboardRecentObjectsSection`

- 支持对象类型标签过滤（可后补）
- MVP 可先不做复杂筛选，仅保留静态分区

---

## 5. 页面二：材料详情 / 阅读页

## 5.1 页面目标

材料详情页要支持用户围绕一份材料完成三件事：

1. 阅读与理解材料
2. 获取 AI 提炼结果
3. 把材料接入案件 / 分析 / 知识链路

---

## 5.2 核心用户任务

### 任务 A：阅读材料正文

用户进入后，先快速理解材料内容。

### 任务 B：触发 AI 处理

用户希望从材料中得到摘要、争点候选、知识候选。

### 任务 C：关联案件或分析

用户希望把这份材料接到现有案件或分析中。

### 任务 D：沉淀知识

用户从材料中确认一条知识点并写入知识库。

---

## 5.3 页面主路径

## 路径 A：阅读材料正文

```text
进入材料详情页
→ 查看标题与元信息
→ 阅读中间正文区
→ 根据需要切换右侧关联或 AI 结果
```

### 阅读区交互建议

- 正文区为页面主区域
- 支持长文滚动
- 支持显示段落级结构（若可解析）
- 支持复制文本
- 支持后续预留选中后触发 AI 动作

---

## 路径 B：触发 AI 处理

```text
进入材料详情页
→ 看到 AI 侧栏
→ 点击“生成摘要” / “识别争点候选” / “提取知识候选”
→ 系统处理
→ 结果回填右侧 AI 区
→ 用户确认、忽略或继续使用结果
```

### AI 动作建议

- `生成摘要`
- `识别争点候选`
- `提取知识候选`
- `推荐关联案件`

### AI 结果交互

每类结果都需要支持：

- `接受`
- `忽略`
- `复制`
- `继续编辑`（若需要）

### 状态切换

- `idle`：尚未处理
- `processing`：显示局部 loading
- `partial-result`：已有部分结果
- `success`：完整结果可交互
- `error`：当前动作失败，可重试

### 交互原则

- AI 区默认不覆盖正文
- AI 处理不阻塞用户继续阅读
- 某个 AI 动作处理时，仅锁定该动作按钮，不锁全侧栏

---

## 路径 C：关联案件

```text
进入材料详情页
→ 在右侧关系区看到“关联案件”
→ 点击“关联案件”
→ 打开搜索弹层 / 抽屉
→ 搜索或选择现有案件
→ 确认关联
→ 右侧列表更新
```

### 交互细节

- 支持搜索案件标题
- 支持显示最近案件快捷选择
- 成功后在关系列表中即时回填

### 成功反馈

- Toast：`已关联到案件`
- 关系列表增加新项

### 错误反馈

- 若重复关联：提示 `该材料已关联此案件`
- 若接口失败：保留弹层并允许重试

---

## 路径 D：关联分析

```text
进入材料详情页
→ 点击“关联分析”
→ 选择某案件下分析
→ 确认
→ 更新相关分析列表
```

### 说明

若分析必须依附案件，则流程中应先选案件，再选分析。

---

## 路径 E：从候选知识创建知识点

```text
进入材料详情页
→ AI 提取出知识候选
→ 用户查看候选条目
→ 点击“创建知识点”
→ 打开简化确认表单
→ 提交
→ 知识创建成功并回填关联区
```

### 最小确认字段

- 标题
- 内容摘要 / 规则表述

### 成功反馈

- Toast：`知识点已创建`
- 右侧“相关知识”区新增条目

---

## 5.4 页面状态设计

## 材料页 `initial-loading`

- 标题骨架
- 正文骨架
- 右侧栏骨架

## 材料页 `processing-source`

适用于文件上传后内容仍在解析中。

### 表现建议

- 正文区显示 `正在处理材料内容`
- 允许用户查看基础元信息
- AI 按钮先禁用或标记待可用

## 材料页 `empty-content`

适用于材料对象已创建，但正文尚无可展示内容。

### 表现建议

- 告知内容尚未抽取完成或为空
- 提供 `重新处理`

## 材料页 `error`

### 页面级错误

- 无法加载材料详情
- 提供 `返回上一页` 与 `重试`

### 局部错误

- AI 处理失败
- 关联动作失败
- 知识创建失败

局部错误不应阻断整页阅读。

---

## 5.5 材料页组件交互要求

### `MaterialContentViewer`

- 长文阅读稳定
- 不因右侧栏刷新导致正文滚动位置丢失
- 后续应支持锚点定位与段落引用扩展

### `MaterialAiSidebar`

- 结果卡片按类别分组
- 每张结果卡支持明确动作
- 不把“AI 已建议”和“用户已采纳”混为一类

### `MaterialRelationsSidebar`

- 分组显示案件 / 分析 / 知识
- 每组支持新增关联入口
- 每项支持点击进入详情

---

## 6. 页面三：案件分析工作区

## 6.1 页面目标

案件工作区是整个产品的主战场，要帮助用户完成：

1. 组织案件信息
2. 构建争点与分析结构
3. 关联证据与知识
4. 借助 AI 推进分析与产出

---

## 6.2 核心用户任务

### 任务 A：浏览并切换案件分析上下文

用户要快速理解当前案件在做什么。

### 任务 B：创建或编辑分析

用户要在工作区中持续写作与迭代分析内容。

### 任务 C：围绕争点挂接材料与知识

用户要让分析有来源、有支撑。

### 任务 D：用 AI 推进下一步

用户希望得到结构建议、争点补全、结论草稿或成果生成。

---

## 6.3 页面主路径

## 路径 A：进入并理解案件工作区

```text
从首页或案件列表进入工作区
→ 看到案件头部信息
→ 左侧看到争点树 / 中间看到分析画布 / 右侧看到 AI 动作
→ 理解当前案件阶段与最近工作状态
```

### 页面首屏必须回答的问题

- 当前是哪个案件？
- 现在主要分析哪个问题？
- 有哪些材料已挂进来？
- 当前分析进行到什么程度？
- 下一步最推荐做什么？

---

## 路径 B：创建新分析

```text
进入工作区
→ 点击“新建分析”
→ 选择分析归属（案件整体 / 某争点）
→ 输入分析标题
→ 创建成功
→ 中间分析编辑区切换到新分析
```

### 最小字段

- 分析标题

### 可选字段

- 所属争点
- 分析类型
- 初始目标

### 成功反馈

- 新分析进入当前上下文
- 编辑器自动聚焦
- 左侧树或分析列表同步更新

---

## 路径 C：编辑分析内容

```text
进入工作区
→ 在中间编辑区输入或修改分析
→ 系统自动保存 / 手动保存
→ 保存成功后更新状态
```

### 编辑器状态建议

- `clean`
- `dirty`
- `saving`
- `saved`
- `save-error`

### 表现建议

- 顶部显示保存状态
- 自动保存频率避免过于激进
- 保存失败后保留草稿，不丢内容

### 成功反馈

- 状态栏显示 `已保存`
- 更新时间刷新

### 失败反馈

- 显示 `保存失败`
- 提供 `重试保存`
- 编辑内容仍保留本地草稿

---

## 路径 D：围绕争点组织分析

```text
进入工作区
→ 在左侧争点树浏览节点
→ 点击某争点节点
→ 中间分析区切换到该争点上下文
→ 右侧 AI / 证据推荐随上下文切换
```

### 争点树节点交互

- 点击切换上下文
- 当前节点高亮
- 支持展开 / 折叠子节点
- 支持后续新增“创建子争点”

### 上下文切换规则

- 若当前编辑器有未保存改动，需明确策略：
  - 自动保存成功后切换，或
  - 弹出确认：`保存并切换` / `放弃修改` / `取消`

MVP 建议：

- 默认先自动保存；保存失败则阻止切换并提示

---

## 路径 E：给分析挂接材料

```text
进入工作区
→ 在证据区点击“添加材料”
→ 打开材料选择抽屉
→ 搜索 / 选择已有材料
→ 确认绑定
→ 材料列表更新
→ 可点击某材料回到材料详情页
```

### 交互细节

- 支持搜索标题
- 支持按最近材料快速选择
- 支持多选（可后补）
- 成功后在证据区显示材料摘要信息

---

## 路径 F：给分析挂接知识

```text
进入工作区
→ 在知识引用区点击“添加知识”
→ 搜索知识点
→ 选择并确认
→ 知识引用列表更新
```

### 知识项展示建议

- 标题
- 类型 / 标签
- 一句话摘要
- 来源数量（可后补）

---

## 路径 G：使用 AI 推进分析

```text
进入工作区
→ 右侧 AI 动作区展示建议动作
→ 用户点击某动作
→ 系统根据当前案件 / 争点 / 分析上下文处理
→ 结果回填为建议块或草稿
→ 用户接受、插入、改写或忽略
```

### AI 动作建议

- `补全争点结构`
- `生成分析提纲`
- `总结已挂材料`
- `生成结论草稿`
- `生成成果初稿`

### AI 结果落点

根据动作不同，结果应进入不同容器：

- 争点类结果 → 左侧建议区或树旁建议面板
- 分析类结果 → 中间编辑区侧边建议块
- 成果类结果 → 新建成果对象或成果预览抽屉

### 交互原则

- AI 结果默认先作为“建议”，不是直接覆盖用户正文
- 用户必须有明确的 `插入` / `替换` / `忽略` 权限
- 生成长内容时要能预览后再采纳

---

## 6.4 页面状态设计

## 工作区 `initial-loading`

- 头部骨架
- 左侧树骨架
- 中间编辑区骨架
- 右侧动作区骨架

## 工作区 `empty-analysis`

适用于案件存在，但尚未创建分析。

### 表现建议

- 中间显示空状态说明
- 主按钮：`创建第一份分析`
- 可附带推荐下一步

## 工作区 `empty-evidence`

分析已创建，但尚无材料。

### 表现建议

- 证据区提示 `尚未关联材料`
- 主入口：`添加材料`

## 工作区 `processing-ai`

- 仅锁定当前 AI 动作卡片
- 其他区域仍可浏览
- 若会生成较长内容，显示处理中状态与预计结果容器

## 工作区 `save-error`

- 编辑器顶部固定提示保存失败
- 用户可以继续编辑，但必须可重试

## 工作区 `page-error`

- 无法加载案件工作区
- 提供 `返回案件列表` / `重试`

---

## 6.5 工作区组件交互要求

### `CaseWorkspaceHeader`

- 固定显示案件标题、阶段、更新时间
- 提供主动作：`新建分析`
- 次动作进入更多菜单

### `CaseIssueTreePanel`

- 支持层级展开折叠
- 节点切换应稳定
- 当前节点与当前分析上下文要保持同步

### `CaseAnalysisEditor`

- 需要稳定承载长文本编辑
- 需要可见保存状态
- 不因右栏刷新导致输入抖动

### `CaseEvidencePanel`

- 显示已关联材料
- 支持新增、跳转、后续可支持移除

### `CaseAiActionSidebar`

- 动作卡片要按目标分组：结构、分析、结论、成果
- 每张结果卡都要有明确采纳方式

---

## 7. 跨页面跳转原则

## 7.1 首页 → 材料页

上传成功或点击最近材料后进入材料详情页。

## 7.2 首页 → 工作区

点击继续案件或新建案件成功后进入案件工作区。

## 7.3 材料页 → 工作区

从关联案件、关联分析、推荐案件入口跳转到工作区。

## 7.4 工作区 → 材料页

点击证据区中的材料项进入材料详情页。

### 原则

跨页跳转时尽量保留上下文：

- 从哪个对象进入
- 当前分析或争点上下文
- 返回路径可理解

后续可考虑：

- 带 query 参数回传上下文
- 最近访问记录恢复

---

## 8. 反馈与确认流建议

## 8.1 成功反馈

适用于：

- 上传材料成功
- 创建案件成功
- 创建分析成功
- 绑定关系成功
- 创建知识成功

### 表现

- 优先使用轻量 toast
- 若触发页面跳转，不必要求用户手动关闭

## 8.2 失败反馈

### 原则

- 就近反馈
- 不用笼统 `操作失败`
- 给出可执行下一步

### 例如

- `上传失败，请检查文件格式后重试`
- `保存失败，当前内容已保留，可再次保存`
- `该材料已关联到当前案件`

## 8.3 风险确认

适用于未来可能的：

- 删除分析
- 解除关键关联
- 覆盖 AI 改写正文
- 归档案件

### 规则

- 必须二次确认
- 明确告知影响范围

---

## 9. MVP 实现优先级

## 9.1 P0 必须实现的交互

- 首页继续案件
- 首页上传材料
- 首页新建案件
- 材料页阅读正文
- 材料页触发摘要生成
- 材料页关联案件
- 工作区创建分析
- 工作区编辑分析
- 工作区关联材料
- 工作区触发 AI 生成分析建议

## 9.2 P1 建议首版加入的交互

- 材料页提取知识候选
- 材料页关联分析
- 工作区关联知识
- 工作区生成成果初稿
- 首页最近对象查看更多

## 9.3 P2 后补

- 段落级引用
- 多选批量关联
- 复杂筛选
- 拖拽重排争点
- 版本对比与回滚

---

## 10. 本阶段结论

### 10.1 原型结论

- 三大页面已经从“页面结构”推进到“关键交互流”
- 主路径、状态切换、反馈机制、局部异步行为已明确
- 前端可以据此开始搭页面骨架、按钮行为、弹层流程与状态管理

### 10.2 产品结论

当前 MVP 主闭环已经足够清晰：

- 首页进入工作
- 材料页完成理解与接入
- 工作区完成分析与生成

### 10.3 工程结论

这份文档已经足够支撑：

- 高保真原型设计
- 前端页面交互实现
- API 联调优先级排序
- 测试用例编写

---

## 11. 下一步建议

建议按以下顺序继续：

1. **OpenAPI / 接口清单 v1**：把 API 草案转成更正式契约
2. **SQL / ORM 初稿 v1**：把数据库草模转成实际 schema
3. 已输出 `前端脚手架初始化建议 v1`（见 `FRONTEND_SCAFFOLD_INIT_V1.md`）
4. **页面状态矩阵 v1**：把 loading / empty / error / success 细化成测试矩阵

如果继续顺推，我建议下一步做：

> **页面状态矩阵 v1**

因为现在信息架构、页面、组件、数据、接口、页面树、交互原型和脚手架方案都已经齐了，下一步最适合把状态设计收敛成实现与测试都能直接使用的矩阵。